import React from 'react';

//this component displays when on the turn.  its main purpose is for the function called in component did mount to initiate dealing the turn card,
class Turn extends React.Component {
  constructor(props) {
    super(props);
  }

  componentDidMount() {
 
 
  }

  render () {
    return (<div>Turn</div>);
  }
}

export default Turn;